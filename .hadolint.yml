# workflow-repo/.hadolint.yaml
trustedRegistries:
  - docker.io

failure-threshold: error

ignored:
  - DL3007   # allow :latest on YOUR trusted images
  - DL3033   # allow yum without version pinning
  - DL4006   # allow pipe without pipefail